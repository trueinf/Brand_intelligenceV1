# OpenAI (required for campaign image generation + TTS voiceover)
OPENAI_API_KEY=

# Gemini (required for: image script when IMAGE_AGENT=gemini; campaign video script; Veo video generation)
GEMINI_API_KEY=

# Clearbit (optional; enriches brand with company name, logo, industry, size, location)
# CLEARBIT_API_KEY=

# SerpAPI (optional; YouTube creatives + Google Trends interest over time)
# SERPAPI_KEY=

# Set to "true" on Netlify (or any short-timeout serverless) to use fast analysis:
# mock data + 2 LLM calls only (no Clearbit/SerpAPI/Google Trends). Avoids 504 timeout.
# FAST_ANALYSIS=true

# PostgreSQL (optional; enables analysis history)
# DATABASE_URL=postgresql://user:pass@localhost:5432/brand_intelligence

# Optional: enable pgvector later
# PGVECTOR_ENABLED=true

# Runway AI (text-to-video for campaign ad video)
# RUNWAY_API_KEY=

# Video generation pipeline (default: openai)
# VIDEO_PROVIDER=openai
# VIDEO_PROVIDER=gemini

# Image prompt agent: "gemini" uses Gemini for image script; unset uses OpenAI.
# IMAGE_AGENT=gemini

# Netlify: set to disable Remotion render (use campaign video job instead). Avoids Chrome/bundle.
# DISABLE_REMOTION=true

# Campaign API — when set, dashboard uses this URL for campaign generation (no Netlify timeout).
# Local: run "npm run server", then set to http://localhost:3001
# Production: set to your Render (or other) backend URL.
# NEXT_PUBLIC_CAMPAIGN_API_URL=http://localhost:3001
# NEXT_PUBLIC_CAMPAIGN_API_URL=https://your-service.onrender.com

# Upstash Redis (required for async campaign jobs; persists job state across requests)
# Required on Netlify for start-campaign and start-campaign-video jobs.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
