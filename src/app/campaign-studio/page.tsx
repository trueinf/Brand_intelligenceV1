import { redirect } from "next/navigation";

/**
 * Campaign Studio route redirects to single-page app (brand search + campaign assets).
 */
export default function CampaignStudioPage() {
  redirect("/");
}
